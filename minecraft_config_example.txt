# Minecraft Server Configuration
# Copy this file and rename to minecraft_config.txt, then fill in your details

# Your Minecraft server IP (use 'localhost' if bot runs on same machine)
MC_HOST=localhost

# RCON port (default is 25575, set in server.properties)
MC_RCON_PORT=25575

# RCON password (set in server.properties as rcon.password)
MC_RCON_PASSWORD=your_rcon_password_here

# Optional: Server chat webhook for real-time chat monitoring
MINECRAFT_WEBHOOK_URL=your_discord_webhook_url_here